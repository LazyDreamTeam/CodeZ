{ "Activer le mode rewrite" : "sudo a2enmod rewrite\r\n\r\nMaintenant, ouvrez le fichier apache2.conf :\r\n\r\nsudo gedit /etc/apache2/apache2.conf\r\n\r\net rajoutez, à la fin de ce dernier, le code suivant afin de bien s’assurer que le module sera activé :\r\n\r\n<ifModule mod_rewrite.c>\r\nRewriteEngine On\r\n</ifModule>\r\n\r\n\r\nDans /etc/apache2/sites-available/default s'arruer que tous les AllowOverride sont a All\r\n\r\nFinalement, redémarrez votre serveur Apache et le tour est joué !\r\n\r\nsudo /etc/init.d/apache2 restart", "Consulter un log en temps réel" : "#consulter le /var/log/syslog\ntail -f /var/log/syslog\n#consulter les X dernière lignes de /var/log/syslog\ntail -X /var/log/syslog", "Résoudre pb  Could not reliably determine the server's fully qualified domain name" : "Dans le fichier apache2.conf \r\ntu rajoutes à la fin de ce fichier la ligne suivante:\r\n\r\nServerName 127.0.1.1 " }